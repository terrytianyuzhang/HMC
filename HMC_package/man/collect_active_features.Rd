% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/51_convergence.R
\name{collect_active_features}
\alias{collect_active_features}
\title{Identify consistently selected features across folds}
\usage{
collect_active_features(
  test_result,
  voting_method = c("majority_voting"),
  group = NULL,
  group_threshold = 1
)
}
\arguments{
\item{test_result}{A list returned by \code{convergence_testing()}, containing fold-wise results in the \code{$fold_data} element.}

\item{voting_method}{Character. Voting rule used to select features. Currently only \code{"majority_voting"} is supported.}

\item{group}{Optional. A named vector assigning each feature to a group. Required for identifying active groups.}

\item{group_threshold}{Integer. Minimum number of selected features in a group for it to be considered active. Default is 1.}
}
\value{
If \code{group} is \code{NULL}, returns a character vector of active feature names.
If \code{group} is provided, returns a list with:
\describe{
\item{active_features}{Character vector of consistently selected features.}
\item{active_groups}{Character vector of active group labels.}
}
}
\description{
Aggregates non-zero features from cross-validation folds and applies majority voting
to identify stable, active features. Optionally returns active feature groups if a
grouping structure is provided.
}

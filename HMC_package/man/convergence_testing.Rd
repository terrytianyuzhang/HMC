% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/51_convergence.R
\name{convergence_testing}
\alias{convergence_testing}
\title{High-dimensional two-stage mean comparison with projection-based testing}
\usage{
convergence_testing(
  control,
  treatment1,
  treatment2,
  pca_method = c("dense_pca", "sparse_pca"),
  classifier_method = c("lasso", "group_lasso"),
  lambda_type = "lambda.1se",
  n_folds = 10,
  group = NULL,
  standardize_feature = TRUE,
  verbose = TRUE
)
}
\arguments{
\item{control}{A matrix or data frame representing the control group. Rows are samples and columns are features.}

\item{treatment1}{A matrix or data frame representing the first treatment group.}

\item{treatment2}{A matrix or data frame representing the second treatment group (comparison target).}

\item{pca_method}{Character. Method for estimating principal components. Must be either \code{"dense_pca"} or \code{"sparse_pca"}.}

\item{classifier_method}{Character. Method for feature selection. Must be either \code{"lasso"} or \code{"group_lasso"}.}

\item{lambda_type}{Character. Type of regularization strength to use from cross-validation. Either \code{"lambda.min"} or \code{"lambda.1se"}.}

\item{n_folds}{Integer. Number of folds for cross-validation. Default is 10.}

\item{group}{Optional. A grouping vector for use with group-lasso, with length equal to the number of columns in \code{control}.}

\item{standardize_feature}{Logical. Whether to normalize features using pooled mean and standard deviation before analysis. Default is \code{TRUE}.}

\item{verbose}{Logical. Whether to print progress messages. Default is \code{TRUE}.}
}
\value{
A list containing:
\describe{
\item{p_value}{Two-sided p-value for the null hypothesis of no mean difference along the constructed projection direction.}
\item{test_statistic}{Aggregated test statistic across folds.}
\item{fold_data}{List of per-fold results including statistics, scores, and projections.}
}
}
\description{
Performs a cross-validated two-stage mean comparison procedure for high-dimensional data.
The method combines sparse classification (lasso or group-lasso) and principal component analysis
to construct interpretable projection directions for hypothesis testing.
}
